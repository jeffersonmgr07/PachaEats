
const PE={base:document.body.dataset.base||'',data:{},cart:JSON.parse(localStorage.getItem('pe_cart')||'[]'),async load(){const files=['restaurants','products','categories','promotions','orders','riders','business-config'];for(const f of files){this.data[f]=await fetch(`${this.base}assets/data/${f}.json`).then(r=>r.json())}this.renderCommon();},money(n){return `S/ ${Number(n||0).toFixed(2)}`},saveCart(){localStorage.setItem('pe_cart',JSON.stringify(this.cart));this.renderCartMini();},add(productId){const p=this.data.products.find(x=>x.id===productId);if(!p)return;const found=this.cart.find(x=>x.id===productId);if(found)found.qty++;else this.cart.push({id:productId,qty:1});this.saveCart();window.dispatchEvent(new CustomEvent('pe:cart'));},remove(productId){this.cart=this.cart.filter(x=>x.id!==productId);this.saveCart();window.dispatchEvent(new CustomEvent('pe:cart'));},cartTotals(){const subtotal=this.cart.reduce((s,i)=>{const p=this.data.products?.find(x=>x.id===i.id);return s+(p?p.price*i.qty:0)},0);const restaurantId=this.cart[0]?this.data.products.find(x=>x.id===this.cart[0].id)?.restaurantId:null;const rest=this.data.restaurants?.find(r=>r.id===restaurantId);const delivery= subtotal>0 ? (rest?.deliveryFee||6):0;return {subtotal,delivery,total:subtotal+delivery}},renderCommon(){this.renderCartMini();const hamb=document.querySelector('.hamb');const nav=document.querySelector('.navlinks');if(hamb&&nav)hamb.onclick=()=>nav.classList.toggle('open');},renderCartMini(){const count=this.cart.reduce((s,i)=>s+i.qty,0);document.querySelectorAll('[data-cart-count]').forEach(el=>el.textContent=count)},restaurantLogo(r){return `${this.base}${r.logo}`},productImage(p){return `${this.base}${p.image}`}}
function header(base=''){return `<header class="topbar"><div class="container"><nav class="nav"><a class="brand" href="${base}index.html"><img src="${base}assets/img/logos/Logo1.png" alt="Pacha Eats"><span>Pacha Eats</span></a><div class="searchbar"><span>🔎</span><input data-search placeholder="Busca restaurantes, platos o promos"></div><button class="hamb">☰</button><div class="navlinks"><a href="${base}productos.html">Platos</a><a href="${base}restaurantes.html">Restaurantes</a><a href="${base}clientes/prime.html">Prime</a><a href="${base}seguimiento-pedido.html">Rastrear</a><a class="cart-pill" href="${base}checkout.html">🛒 <span data-cart-count>0</span></a></div></nav></div></header>`}
function footer(base=''){return `<footer class="footer"><div class="container footer-grid"><div><h3>Pacha Eats</h3><p>Marketplace local para restaurantes, clientes y repartidores en soles peruanos.</p></div><div><h4>Clientes</h4><a href="${base}productos.html">Pedir comida</a><a href="${base}clientes/prime.html">Pacha Prime</a><a href="${base}seguimiento-pedido.html">Seguir pedido</a></div><div><h4>Aliados</h4><a href="${base}restaurantes/login.html">Acceso restaurantes</a><a href="${base}repartidores/login.html">Acceso repartidores</a><a href="${base}restaurantes/registro.html">Afiliar restaurante</a></div><div><h4>Operación</h4><a href="${base}admin/login.html">Admin</a><a href="${base}docs/ROADMAP.md">Roadmap</a><a href="${base}backend-ready/API_CONTRACT.md">API futura</a></div></div></footer>`}
function mountLayout(base=''){document.body.insertAdjacentHTML('afterbegin',header(base));document.body.insertAdjacentHTML('beforeend',footer(base))}
function restaurantCard(r,base=''){return `<article class="restaurant-card"><img class="restaurant-cover" src="${base}${r.cover}" alt="${r.name}"><div class="restaurant-row"><img class="restaurant-logo" src="${base}${r.logo}" alt="${r.name}"><div><h3>${r.name}</h3><div class="meta"><span>⭐ ${r.rating}</span><span>${r.deliveryTime}</span><span>${PE.money(r.deliveryFee)}</span></div></div></div><div class="meta">${r.tags.map(t=>`<span class="badge ${t==='Prime'?'yellow':''}">${t}</span>`).join('')} ${r.isOpen?'<span class="badge green">Abierto</span>':'<span class="badge dark">Cerrado</span>'}</div><p>${r.category} · ${r.address}</p><a class="btn btn-primary" href="${base}restaurante.html?id=${r.id}">Ver restaurante</a></article>`}
function productCard(p,base=''){const r=PE.data.restaurants.find(x=>x.id===p.restaurantId);return `<article class="product-card" data-name="${p.name.toLowerCase()} ${r?.name.toLowerCase()}"><img src="${base}${p.image}" alt="${p.name}"><div class="meta"><span>${r?.name||''}</span>${p.primeDeal?'<span class="badge yellow">Prime deal</span>':''}</div><h3>${p.name}</h3><p>${p.description}</p><div class="price-row"><div><span class="price">${PE.money(p.price)}</span>${p.before?`<span class="before">${PE.money(p.before)}</span>`:''}</div><button class="btn btn-primary" onclick="PE.add('${p.id}')">Agregar</button></div></article>`}
function renderCartBox(target){const el=document.querySelector(target);if(!el)return;const items=PE.cart.map(i=>{const p=PE.data.products.find(x=>x.id===i.id);return p?`<div class="cart-line"><span>${i.qty}× ${p.name}</span><strong>${PE.money(i.qty*p.price)}</strong><button class="btn btn-soft" onclick="PE.remove('${p.id}')">×</button></div>`:''}).join('');const t=PE.cartTotals();el.innerHTML=`<h3>Tu pedido</h3>${items||'<div class="empty">Tu carrito está vacío.</div>'}<div class="cart-line"><span>Subtotal</span><strong>${PE.money(t.subtotal)}</strong></div><div class="cart-line"><span>Delivery</span><strong>${PE.money(t.delivery)}</strong></div><div class="cart-total"><span>Total</span><strong>${PE.money(t.total)}</strong></div><a class="btn btn-primary" style="width:100%" href="${PE.base}checkout.html">Continuar</a>`}
window.PE=PE;window.mountLayout=mountLayout;window.restaurantCard=restaurantCard;window.productCard=productCard;window.renderCartBox=renderCartBox;
